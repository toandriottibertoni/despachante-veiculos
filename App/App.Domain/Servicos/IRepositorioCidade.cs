using App.Domain.Entidades;
using App.Domain.Servicos.Base;
using System;
using System.Collections.Generic;

namespace App.Domain.Servicos
{
    public interface IRepositorioCidade : IRepositorio<Cidade>
    {
        //bool Add(Cidade Cidade);
        //bool Update(Cidade cidade);
        //bool Delete(int CidadeId);
        //Cidade Get(int CidadeId);
        //void Save();
        //List<Cidade> GetAll();
        //List<Cidade> GetWith(Func<Cidade, bool> condition);
        //Cidade GetSingleWith(Func<Cidade, bool> condition);
    }
}
